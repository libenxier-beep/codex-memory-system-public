# Indexes Example

This directory represents lightweight current/recent lookup entrypoints.

- Keep `current_context` and `recent_activity` style files here.
- Query indexes before reading deeper session or evidence files.
