# Short-Term Memory (Legacy)

This example tree keeps `short_term/` only as a retirement marker.

- session summaries moved to `examples/sanitized-memory/memory-sidecar/sessions/`
- candidate behavior is represented in `rollout_summaries/` in the full architecture, not as an active directory here
